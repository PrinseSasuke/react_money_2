REACT_APP_API_URL=http://localhost:4000/api
# Используется docker-compose.yml для backend-контейнера (JWT_SECRET уже
# задавался так же, просто не был здесь документирован)
JWT_SECRET=replace_with_a_long_random_string
# Токен бота от @BotFather. Пусто/не задан — Telegram-бот в backend не
# запускается.
TELEGRAM_BOT_TOKEN=
